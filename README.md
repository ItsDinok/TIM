# Task Inference Mechanisms (TIM)/Task Estimation Gates (TEG)
### (Name Pending)

This is a python package that implements my TIM/TEG architecture in order to prevent catastrophic forgetting in class-incremental learning systems. It is currently a sprawling mess, but this will be fixed in due time. 
